/**
 * un operador es un simbolo que le dice al compilador
 * que debe realizar una tarea
 * matermatica, relacional o lógica
 * y debe de producti un resultado
 */
main(){
  //Básicos
  int a = 10 + 5;
  a = 20 - 10;
  a = 10 * 2;

  double b = 10 / 2;
  b = 10.0 % 3; // % es para el sobrante de la división
  b = -b; // es usado para cambiar el valor de la expresión

  int c = 10 /3;

  int d = 1;
  d++; // ++2
  d--; // --1

  d += 2;
  d -= 2;
}